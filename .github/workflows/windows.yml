name: Build and Release Windows Exe

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller pillow

      - name: Build with PyInstaller
        run: |
          pyinstaller --name "WebAppCore-Desktop" --onefile --windowed --icon="webappcore/icon.png" --add-data "webappcore/icon.png;webappcore" --add-data "webappcore/panel;webappcore/panel" --add-data ".env.example;." webappcore_app.py

      - name: Rename Executable
        run: |
          $tag = if ("${{ github.ref_name }}" -ne "") { "${{ github.ref_name }}" } else { "manual" }
          mv dist/WebAppCore-Desktop.exe "dist/WebAppCore-Desktop-$tag.exe"
        shell: pwsh

      - name: Upload artifact (manual runs)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: WebAppCore-Desktop-exe
          path: dist/WebAppCore-Desktop-*.exe

      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          prerelease: ${{ endsWith(github.ref_name, '-dev') }}
          files: dist/WebAppCore-Desktop-${{ github.ref_name }}.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
